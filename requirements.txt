# TelecomLens dependencies
# Minimum versions — pip resolves the latest compatible build.
# sqlalchemy>=2.0.36 is required for Python 3.13+ (fixes __firstlineno__ issue).

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
sqlalchemy>=2.0.36
pydantic-settings>=2.3.0
python-docx>=1.1.0
openpyxl>=3.1.0
httpx>=0.25.0
